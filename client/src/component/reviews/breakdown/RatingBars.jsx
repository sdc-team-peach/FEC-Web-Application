import React from 'react';
import BarContatiner from './Breakdown.styles';

function RatingBars({
  value, max, color, width,
}) {
  return (
    <div>
      rating bars!
    </div>
  );
}

export default RatingBars;
{/* <BarContatiner color="#e0e0e0" width={width}>
<progress value={value} max={max} />
<span>3.5</span>
</BarContatiner> */}